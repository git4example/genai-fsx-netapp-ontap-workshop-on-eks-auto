# This file previously contained FSx for Lustre sysprep resources (HSM restore job,
# Lustre CSI driver, sysprep PV/PVC). Those resources have been removed as part of
# the migration to FSx for ONTAP.
#
# Model loading for FSx ONTAP is handled by a Kubernetes Job (model-loading-job.yaml)
# applied during the workshop install process, not via Terraform.
